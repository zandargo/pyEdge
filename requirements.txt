# Runtime
PyQt5
PyQt-Fluent-Widgets
pywin32
ezdxf        # DXF file reading (sheet metal DXF area calculation)
shapely      # polygon containment-tree for hole detection

# Build / packaging
pyinstaller
pillow          # required to convert PNG → ICO for the Windows executable icon
nuitka          # recommended build for fastest startup (compiles Python → native C)
                # also requires MSVC (Visual Studio Build Tools) or MinGW-w64
